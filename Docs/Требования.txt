Запросы на сервер:

1)JoinLobby
request{
}
response{
token
}

2)UpdateLobby
request{
token
}
response{
token
status: WAITING/READY/ERROR
countOfPlayers
}

3)GetGameStatus()		//Таки внимание на название метода
request{
token
}

response{
isDone	//Получен ли SetGameStatus от этого игрока на этом ходу
countOfPlayers //кол-во игроков, которые уже отправили SetGameStatus на этом этапе
stage: [1..4]		//В зависимости от стадии один из следующих ответов
1)isHead, card#[0..5], score
2)text	(текст ассоциации ведущего)
3)card#[0..5]	(Карты выбранные на 2 ходу)
4)card#[0..5], vote#[0..5], сardOfHead (ID карты ведущего)
}

4)SetGameStatus			//Таки внимание. ИМХО так лучше, что бы прослеживалась связь get/set
response{
status: OK/ERROR
}

request{
1) (isHead == true) token, card (карта выбранная ведущим), text (текст ассоциации)
2) (isHead == false) token, card
3) (isHead == false) token, votedCard
}

Общее:

	Стадии:
	1)Раздача карт(Клиенту отправляются все карты которые есть у него в руке) Отправка общего кол-ва очков игроку.
	Ведущий в ответ отправляет текст ассоциации и выбранную им карту
	2)Игроки (!= ведущий) получают текст ассоциации
	Отправляют в ответ карту
	3)Игроки получают все выбранные в этом ходу карты.
	В ответ отправляют ID карты за которую голосуют
	4)ВСЕ получают результаты голосования и ID карты ведущего. Так же можно (для будущих версий) уже прикрутить отправку deltaScore
	В ответ ничего никто не отправляет
	
ВАЖНО: Выше указано всё форматирование названий. Прошу считать его эталоном для имагинариума. И если честно мне плевать как принято именовать ключи в JSON.

И вместо заключения... Текст "Запросы на сервер" немного подредактированный (и дополненныей) копипаст из файла ServerAPI. Не так сложно было следовать ему или хотя бы изменять, да?
Последнее изменение ServerAPI было 6 декабря. Небольшие правки от тебя.

Надеюсь ты не будешь костылить.